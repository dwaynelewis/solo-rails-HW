<%= form_with(model: aide, local: true) do |form| %>
  <% if aide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aide.errors.count, "error") %> prohibited this aide from being saved:</h2>

      <ul>
      <% aide.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :aide_name %>
  </div>

  <div class="field">
    <%= form.label :slack_name %>
    <%= form.text_field :slack_name, id: :aide_slack_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
